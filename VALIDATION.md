# 视觉候鸟 1.0 验证记录

日期：2026-09-17。运行环境：macOS，Codex 内置 Chromium 浏览器。示例素材为项目内置 AI 玉兰图与 AI 城市图，未把个人照片打包。

## 已验证

- 新 renderer 只包含迁徙机制，没有语义、重力、侵蚀或声音方向的初始化及计算。
- 21 组渲染与原 v0.8 在相同源图、800 px 长边、进度 73%、时刻 1.8 秒下逐像素一致，差异通道数均为 0。
- 对比包括蝴蝶、滑稽、庆祝表情、肤色与家庭组合、纹理燕子/枫叶、自定义反向路径、保护与允许笔刷、十种构图和半幅完整显示。
- 可编辑项目往返恢复源图与参数；旧 v0.7 项目省略末端尺寸时恢复为 140%；损坏蒙版被拒绝且不覆盖当前编辑。
- 普通与透明 PNG 分别生成 1280 × 1600 图像，检查角落 alpha 为 255 / 0。
- 五种视口 320×568、390×844、430×932、844×390、1440×900 下，逐一检查五个调节页，未发生页面横向溢出，画布和工具导航均在视口内。
- 在 390×844 浏览器中实际操作照片右上布局、自由拖动、Emoji 选择器粘贴“🥳”、生成 PNG 预览。
- 通过文件选择器上传 AI 城市 PNG，编辑器成功载入照片。
- 录制并生成可播放 MP4 预览，浏览器读取元数据为约 5.96 秒，640×800 px。尚未验证系统相册的最终保存行为。
- 自带 Node 静态服务器在 127.0.0.1:8787 独立启动并成功打开编辑器，不依赖原 8765 服务。
- `npm run check`：renderer、app、picker、serve 的 JavaScript 语法检查通过。

以上逐像素记录来自拆分时的原版对比，原七方向工具和对比页未打入此仓库。当前 `/tests/smoke.html` 可独立检查基本渲染和项目恢复；仓库与部署检查见 `docs/PACKAGE-CHECKS.md`。

## 待真机验收

浏览器缩放/视口测试不等于手机操作系统测试。以下尚未声称通过：

- iPhone Safari 和 Android Chrome 的真实手指拖动、长时间播放、软键盘、安全区域与旋转屏幕。
- 系统分享弹窗、下载目录与“存储到相册”的完整流程。
- 老款手机的内存、帧率、大图导出与录制格式支持。
- 不同设备 Emoji 字体的外观和新字符覆盖。

本记录来自本地独立版验收。线上发布状态见仓库 Actions；公开入口为 `https://liamgvchi.github.io/gc-migration-studio/`。本机 127.0.0.1 链接不能直接在另一台手机打开，同 Wi-Fi 启动方式见 README。
